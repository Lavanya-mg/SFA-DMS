<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Standard SFA expense types are synced idempotently (upsert by name +
         remove legacy demo types) so this never duplicates or clashes on code
         with types created in the UI. See sfa.expense.type._seed_sync_types. -->
    <function model="sfa.expense.type" name="_seed_sync_types"/>
</odoo>
